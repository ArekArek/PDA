<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:editTextStyle">@style/editTextAppStyle</item>
        <item name="android:buttonStyle">@style/buttonAppStyle</item>
        <item name="android:buttonStyleToggle">@style/buttonAppStyle</item>
        <item name="android:typeface">monospace</item>
    </style>

    <style name="editTextAppStyle" parent="Base.Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_drawable</item>
        <item name="android:textColor">@color/backgroundLighter</item>
        <item name="android:textColorHint">@color/backgroundLight</item>
        <item name="android:singleLine">true</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="buttonAppStyle" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background_selector</item>
        <item name="android:textColor">@color/background</item>
    </style>

</resources>
